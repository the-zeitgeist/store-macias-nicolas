import CircularProgress from '@material-ui/core/CircularProgress'

const LocalCircularProgress = () => (
  <div className="center">
    <CircularProgress />
  </div>
);

export default LocalCircularProgress;